const spain = {
    language: "Spanish",
    continent: 'Europe'
};
  
// Template literal with multi-line and string interpolation
const descriptionSpain = `In Spain the language is ${spain.language}!
Spain it's in ${spain.continent}!`;

console.log(descriptionSpain);

// `` Are always need it for this